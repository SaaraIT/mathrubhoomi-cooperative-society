<div class="container-fluid">
  <div class="row">
    <div class="col-12">
      <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h2">Branch Management</h1>
        <%= link_to "New Branch", admin_new_branch_path, class: "btn btn-success" %>
      </div>
    </div>
  </div>

  <div class="row mb-2">
    <div class="col-12 text-end">
      <span class="text-muted">Total: <%= @branches.count %> branches</span>
    </div>
  </div>

  <div class="row">
    <div class="col-12">
      <div class="card">
        <div class="card-body">
          <div class="table-responsive">
            <table class="table table-striped">
              <thead>
                <tr>
                  <th>Branch Name</th>
                  <th>Manager</th>
                  <th>Address</th>
                  <th>Phone</th>
                  <th>R.No</th>
                  <th>Users</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% @branches.each do |branch| %>
                  <tr>
                    <td>
                      <strong><%= branch.name %></strong>
                    </td>
                    <td>
                      <% if branch.manager_name.present? %>
                        <%= branch.manager_name %>
                      <% else %>
                        <span class="text-muted">-</span>
                      <% end %>
                    </td>
                    <td>
                      <small class="text-muted"><%= truncate(branch.english_address, length: 40) %></small>
                    </td>
                    <td><%= branch.phone_no || '-' %></td>
                    <td><%= branch.r_no || '-' %></td>
                    <td>
                      <span class="badge bg-info"><%= branch.users.count %></span>
                    </td>
                    <td>
                      <%= link_to "View", admin_show_branch_path(branch), class: "btn btn-sm btn-outline-primary" %>
                      <%= link_to "Edit", admin_edit_branch_path(branch), class: "btn btn-sm btn-outline-secondary" %>
                      <%= link_to "Delete", admin_destroy_branch_path(branch),
                            method: :delete,
                            data: { confirm: "Are you sure? This will delete the branch permanently." },
                            class: "btn btn-sm btn-outline-danger" %>
                    </td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
